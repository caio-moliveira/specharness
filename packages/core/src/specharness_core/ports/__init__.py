"""Ports: contracts the adapters implement (ADR-001).

A port describes *what* the domain needs, never *how* it is obtained. Nothing
in this package may import a framework, a driver or perform I/O.
"""
